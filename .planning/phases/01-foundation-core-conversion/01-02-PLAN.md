---
phase: 01-foundation-core-conversion
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - app/(marketing)/layout.tsx
  - app/components/Header.tsx
  - app/components/MobileNav.tsx
  - app/components/Footer.tsx
  - app/components/EmergencyBadge.tsx
autonomous: true

must_haves:
  truths:
    - "Emergency hotline click-to-call appears in header"
    - "24/7 Emergency Response badge is visible"
    - "Navigation works on mobile with hamburger menu"
    - "Layout renders correctly on all breakpoints"
  artifacts:
    - path: "app/components/Header.tsx"
      provides: "Site header with navigation and emergency hotline"
      exports: ["Header"]
    - path: "app/components/EmergencyBadge.tsx"
      provides: "Fixed position 24/7 badge"
      exports: ["EmergencyBadge"]
    - path: "app/(marketing)/layout.tsx"
      provides: "Marketing pages layout wrapper"
      exports: ["default"]
  key_links:
    - from: "app/(marketing)/layout.tsx"
      to: "app/components/Header.tsx"
      via: "component import"
      pattern: "import.*Header"
    - from: "app/components/Header.tsx"
      to: "tel:"
      via: "click-to-call link"
      pattern: "href=\"tel:"
---

<objective>
Create shared layout components including Header with emergency hotline, Footer, EmergencyBadge, and mobile navigation.

Purpose: Establish the site-wide layout structure that wraps all marketing pages. The Header and EmergencyBadge are critical for the emergency response differentiator - they must be visible on every page.

Output: Complete layout system with responsive navigation and always-visible emergency contact options.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-core-conversion/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Header with emergency hotline and MobileNav</name>
  <files>
    app/components/Header.tsx
    app/components/MobileNav.tsx
  </files>
  <action>
Create app/components/Header.tsx as a Server Component with embedded client MobileNav:

```typescript
import Link from 'next/link'
import { MobileNav } from './MobileNav'

const navLinks = [
  { href: '/', label: 'Home' },
  { href: '/services', label: 'Services' },
  { href: '/contact', label: 'Contact' },
]

export function Header() {
  const emergencyPhone = process.env.NEXT_PUBLIC_EMERGENCY_PHONE || '+1-555-123-4567'

  return (
    <header className="sticky top-0 bg-white shadow-sm z-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="flex items-center justify-between h-16">
          {/* Logo */}
          <Link href="/" className="font-bold text-xl text-brand-dark">
            Red Leader
          </Link>

          {/* Desktop Navigation */}
          <nav className="hidden lg:flex items-center gap-8">
            {navLinks.map((link) => (
              <Link
                key={link.href}
                href={link.href}
                className="text-brand-gray hover:text-brand-dark transition-colors"
              >
                {link.label}
              </Link>
            ))}
          </nav>

          {/* Emergency Hotline - Always Visible */}
          <div className="flex items-center gap-4">
            <a
              href={`tel:${emergencyPhone.replace(/[^+\d]/g, '')}`}
              className="flex items-center gap-2 text-brand-red font-semibold hover:text-brand-red-dark transition-colors"
              aria-label={`Call emergency hotline at ${emergencyPhone}`}
            >
              <svg
                className="w-5 h-5"
                fill="none"
                stroke="currentColor"
                viewBox="0 0 24 24"
                aria-hidden="true"
              >
                <path
                  strokeLinecap="round"
                  strokeLinejoin="round"
                  strokeWidth={2}
                  d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"
                />
              </svg>
              <span className="hidden sm:inline">Emergency:</span>
              <span>{emergencyPhone}</span>
            </a>

            {/* Mobile Nav Toggle */}
            <MobileNav links={navLinks} />
          </div>
        </div>
      </div>
    </header>
  )
}
```

Create app/components/MobileNav.tsx as a Client Component:

```typescript
'use client'

import { useState, useEffect } from 'react'
import { usePathname } from 'next/navigation'
import Link from 'next/link'

interface NavLink {
  href: string
  label: string
}

interface MobileNavProps {
  links: NavLink[]
}

export function MobileNav({ links }: MobileNavProps) {
  const [isOpen, setIsOpen] = useState(false)
  const pathname = usePathname()

  // Close menu on navigation
  useEffect(() => {
    setIsOpen(false)
  }, [pathname])

  // Prevent body scroll when menu is open
  useEffect(() => {
    if (isOpen) {
      document.body.style.overflow = 'hidden'
    } else {
      document.body.style.overflow = ''
    }
    return () => {
      document.body.style.overflow = ''
    }
  }, [isOpen])

  return (
    <>
      <button
        onClick={() => setIsOpen(!isOpen)}
        className="lg:hidden p-2 -mr-2"
        aria-label="Toggle menu"
        aria-expanded={isOpen}
      >
        <div className="space-y-1.5">
          <span
            className={`block w-6 h-0.5 bg-brand-dark transition-transform ${
              isOpen ? 'rotate-45 translate-y-2' : ''
            }`}
          />
          <span
            className={`block w-6 h-0.5 bg-brand-dark transition-opacity ${
              isOpen ? 'opacity-0' : ''
            }`}
          />
          <span
            className={`block w-6 h-0.5 bg-brand-dark transition-transform ${
              isOpen ? '-rotate-45 -translate-y-2' : ''
            }`}
          />
        </div>
      </button>

      {isOpen && (
        <div className="fixed inset-0 z-40 lg:hidden">
          {/* Backdrop */}
          <div
            className="fixed inset-0 bg-black/50"
            onClick={() => setIsOpen(false)}
            aria-hidden="true"
          />

          {/* Menu Panel */}
          <nav className="fixed top-0 right-0 h-full w-64 bg-white shadow-xl p-6 pt-20">
            <ul className="space-y-4">
              {links.map((link) => (
                <li key={link.href}>
                  <Link
                    href={link.href}
                    className={`block py-2 text-lg ${
                      pathname === link.href
                        ? 'text-brand-red font-semibold'
                        : 'text-brand-dark hover:text-brand-red'
                    }`}
                  >
                    {link.label}
                  </Link>
                </li>
              ))}
            </ul>
          </nav>
        </div>
      )}
    </>
  )
}
```
  </action>
  <verify>
    Header renders with logo, nav links, and emergency phone
    Click-to-call link has correct tel: href
    Mobile hamburger menu opens/closes on tap
    Menu closes automatically on navigation
    Body scroll locks when mobile menu is open
  </verify>
  <done>
    Header component with emergency hotline click-to-call
    Mobile navigation with hamburger menu
    Navigation closes on route change
    Accessible with proper aria attributes
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Footer, EmergencyBadge, and marketing layout</name>
  <files>
    app/components/Footer.tsx
    app/components/EmergencyBadge.tsx
    app/(marketing)/layout.tsx
  </files>
  <action>
Create app/components/Footer.tsx:

```typescript
import Link from 'next/link'

export function Footer() {
  const currentYear = new Date().getFullYear()
  const emergencyPhone = process.env.NEXT_PUBLIC_EMERGENCY_PHONE || '+1-555-123-4567'

  return (
    <footer className="bg-brand-dark text-white">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
        <div className="grid grid-cols-1 md:grid-cols-3 gap-8">
          {/* Company Info */}
          <div>
            <h3 className="font-bold text-lg mb-4">Red Leader</h3>
            <p className="text-gray-400">
              Emergency infrastructure rescue and cloud modernization for enterprises.
            </p>
          </div>

          {/* Quick Links */}
          <div>
            <h3 className="font-bold text-lg mb-4">Quick Links</h3>
            <ul className="space-y-2">
              <li>
                <Link href="/services" className="text-gray-400 hover:text-white transition-colors">
                  Services
                </Link>
              </li>
              <li>
                <Link href="/contact" className="text-gray-400 hover:text-white transition-colors">
                  Contact
                </Link>
              </li>
            </ul>
          </div>

          {/* Emergency Contact */}
          <div>
            <h3 className="font-bold text-lg mb-4">Emergency Response</h3>
            <p className="text-gray-400 mb-2">24/7 emergency support available</p>
            <a
              href={`tel:${emergencyPhone.replace(/[^+\d]/g, '')}`}
              className="text-brand-red font-semibold hover:text-red-400 transition-colors"
            >
              {emergencyPhone}
            </a>
          </div>
        </div>

        <div className="border-t border-gray-700 mt-8 pt-8 text-center text-gray-400">
          <p>&copy; {currentYear} Red Leader. All rights reserved.</p>
        </div>
      </div>
    </footer>
  )
}
```

Create app/components/EmergencyBadge.tsx:

```typescript
export function EmergencyBadge() {
  return (
    <div className="fixed bottom-4 right-4 z-50">
      <div className="bg-brand-red text-white px-4 py-2 rounded-full shadow-lg flex items-center gap-2">
        {/* Animated pulse indicator */}
        <span className="relative flex h-3 w-3">
          <span className="animate-ping absolute inline-flex h-full w-full rounded-full bg-red-400 opacity-75" />
          <span className="relative inline-flex rounded-full h-3 w-3 bg-red-300" />
        </span>
        <span className="font-semibold text-sm whitespace-nowrap">
          24/7 Emergency Response
        </span>
      </div>
    </div>
  )
}
```

Create app/(marketing)/layout.tsx to wrap all marketing pages:

```typescript
import { Header } from '@/app/components/Header'
import { Footer } from '@/app/components/Footer'
import { EmergencyBadge } from '@/app/components/EmergencyBadge'

export default function MarketingLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <div className="min-h-screen flex flex-col">
      <Header />
      <main className="flex-1">
        {children}
      </main>
      <Footer />
      <EmergencyBadge />
    </div>
  )
}
```

Move the placeholder page into the marketing route group:
- Move app/page.tsx to app/(marketing)/page.tsx (the file content stays the same)
  </action>
  <verify>
    Footer renders with links and emergency contact
    EmergencyBadge appears fixed in bottom-right corner
    Badge pulse animation works
    Marketing layout wraps content with header/footer
    All components visible at http://localhost:3000
  </verify>
  <done>
    Footer with company info and emergency contact
    EmergencyBadge with animated pulse indicator
    Marketing layout combining header, footer, and badge
    Route group structure ready for marketing pages
  </done>
</task>

</tasks>

<verification>
After completing all tasks:

1. Run `npm run dev` and visit http://localhost:3000
2. Verify Header shows: logo, nav links, emergency phone with click-to-call
3. Resize browser to mobile width - hamburger menu should appear
4. Click hamburger - menu should slide in, click link - menu should close
5. Scroll down - Header should remain sticky at top
6. Verify EmergencyBadge is fixed in bottom-right with pulse animation
7. Verify Footer shows at bottom with emergency contact
8. Test click-to-call link on mobile device (should open phone dialer)
</verification>

<success_criteria>
- Header visible on all pages with emergency hotline (EMER-02)
- 24/7 Emergency Response badge visible on all pages (EMER-01)
- Mobile navigation functional with hamburger menu (TECH-03)
- Footer with contact information
- Layout system ready for page content
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-core-conversion/01-02-SUMMARY.md`
</output>
