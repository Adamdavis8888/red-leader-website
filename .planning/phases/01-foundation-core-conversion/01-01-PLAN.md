---
phase: 01-foundation-core-conversion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - postcss.config.mjs
  - next.config.ts
  - app/globals.css
  - app/layout.tsx
  - app/page.tsx
  - .env.local.example
  - .gitignore
autonomous: true

must_haves:
  truths:
    - "Project builds successfully with npm run build"
    - "Development server starts with npm run dev"
    - "Tailwind CSS classes render correctly"
    - "TypeScript compilation succeeds without errors"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies and scripts"
      contains: "next"
    - path: "app/layout.tsx"
      provides: "Root layout with font configuration"
      exports: ["default"]
    - path: "app/globals.css"
      provides: "Tailwind v4 configuration"
      contains: "@import \"tailwindcss\""
  key_links:
    - from: "app/layout.tsx"
      to: "app/globals.css"
      via: "CSS import"
      pattern: "import.*globals.css"
---

<objective>
Initialize Next.js 16 project with Tailwind v4 and TypeScript configuration.

Purpose: Establish the foundational project structure that all subsequent plans build upon. This creates the development environment and core configuration files.

Output: Working Next.js development environment with Tailwind v4 styling ready for component development.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-core-conversion/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js 16 project with dependencies</name>
  <files>
    package.json
    tsconfig.json
    next.config.ts
    postcss.config.mjs
    .gitignore
  </files>
  <action>
Create a new Next.js 16 project using create-next-app with TypeScript and App Router:

```bash
npx create-next-app@latest . --typescript --tailwind --app --src-dir=false --import-alias="@/*" --use-npm
```

After initialization, install additional dependencies:
```bash
npm install react-calendly zod
```

Verify package.json contains:
- next@16.x (or latest)
- react@19.x
- tailwindcss@4.x
- @tailwindcss/postcss

Update next.config.ts to use modern configuration:
```typescript
import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  // Enable React strict mode for development
  reactStrictMode: true,
}

export default nextConfig
```

Ensure postcss.config.mjs uses Tailwind v4 configuration:
```javascript
export default {
  plugins: {
    '@tailwindcss/postcss': {},
  },
}
```

Note: create-next-app with --tailwind flag should set up Tailwind v4 automatically. If it creates v3 config (tailwind.config.js), delete it and update globals.css per Task 2.
  </action>
  <verify>
    npm run build succeeds without errors
    npm run dev starts server on localhost:3000
  </verify>
  <done>
    Next.js 16 project initialized with all dependencies installed
    TypeScript configured correctly
    Tailwind v4 PostCSS plugin configured
  </done>
</task>

<task type="auto">
  <name>Task 2: Configure Tailwind v4 with brand colors and root layout</name>
  <files>
    app/globals.css
    app/layout.tsx
    app/page.tsx
    .env.local.example
  </files>
  <action>
Update app/globals.css to use Tailwind v4 CSS-first configuration:

```css
@import "tailwindcss";

@theme {
  /* Brand colors - Red Leader emergency/rescue theme */
  --color-brand-red: #dc2626;
  --color-brand-red-dark: #b91c1c;
  --color-brand-dark: #1f2937;
  --color-brand-gray: #6b7280;

  /* Custom spacing for consistent layout */
  --spacing-section: 5rem;

  /* Typography */
  --font-sans: var(--font-inter), ui-sans-serif, system-ui, sans-serif;
}

/* Emergency pulse animation for badge */
@keyframes emergency-pulse {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.7; }
}
```

Update app/layout.tsx to use next/font and set up root structure:

```typescript
import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({
  subsets: ['latin'],
  variable: '--font-inter',
})

export const metadata: Metadata = {
  title: 'Red Leader - Emergency Infrastructure Rescue',
  description: '24/7 emergency response when your systems fail. Expert infrastructure rescue, cloud modernization, and high-availability solutions.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" className={inter.variable}>
      <body className="font-sans antialiased">
        {children}
      </body>
    </html>
  )
}
```

Create a minimal app/page.tsx placeholder:

```typescript
export const dynamic = 'force-static'

export default function HomePage() {
  return (
    <main className="min-h-screen flex items-center justify-center">
      <div className="text-center">
        <h1 className="text-4xl font-bold text-brand-dark">Red Leader</h1>
        <p className="text-brand-gray mt-2">Emergency Infrastructure Rescue</p>
      </div>
    </main>
  )
}
```

Create .env.local.example for documentation:
```
# Calendly configuration
NEXT_PUBLIC_CALENDLY_URL=https://calendly.com/your-username/consultation

# Emergency hotline
NEXT_PUBLIC_EMERGENCY_PHONE=+1-555-123-4567

# Contact form (Phase 2+ - email service)
# RESEND_API_KEY=your_resend_api_key
```
  </action>
  <verify>
    npm run dev shows styled placeholder page
    Browser dev tools show Inter font loading
    Tailwind custom colors work (text-brand-red visible if added)
    No console errors about fonts or CSS
  </verify>
  <done>
    Tailwind v4 configured with brand theme
    Root layout uses next/font for optimal font loading
    Placeholder homepage renders with styling
    Environment variable template created
  </done>
</task>

</tasks>

<verification>
After completing all tasks:

1. Run `npm run build` - should complete without errors
2. Run `npm run dev` - should start on localhost:3000
3. Visit http://localhost:3000 - should show styled placeholder
4. Check browser Network tab - Inter font should load from optimized source
5. Inspect element - Tailwind classes should apply correctly
</verification>

<success_criteria>
- Next.js 16 development environment fully functional
- Tailwind v4 CSS-first configuration active with brand colors
- TypeScript compilation passes
- Font optimization working via next/font
- Project ready for component development in subsequent plans
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-core-conversion/01-01-SUMMARY.md`
</output>
