---
phase: 04-blog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/data/blog-posts.ts
  - app/(marketing)/blog/page.tsx
autonomous: true

must_haves:
  truths:
    - "Visitor can view blog listing page with all posts"
    - "Blog posts display title, excerpt, published date, and category"
    - "Posts are sorted by publish date (newest first)"
  artifacts:
    - path: "app/data/blog-posts.ts"
      provides: "BlogPost type and initial posts data"
      exports: ["BlogPost", "blogPosts", "getBlogPostBySlug", "getCategories"]
    - path: "app/(marketing)/blog/page.tsx"
      provides: "Blog listing page with post cards"
      min_lines: 80
  key_links:
    - from: "app/(marketing)/blog/page.tsx"
      to: "app/data/blog-posts.ts"
      via: "import blogPosts"
      pattern: "import.*blogPosts.*from.*blog-posts"
---

<objective>
Create the blog data file with 2-3 initial posts and the blog listing page.

Purpose: Establish thought leadership content foundation with SEO-ready blog infrastructure.
Output: Blog data types, initial post content, and listing page displaying all posts.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
# Existing patterns to follow
@/Users/adamdavis/red-leader-website/app/data/services.ts
@/Users/adamdavis/red-leader-website/app/data/case-studies.ts
@/Users/adamdavis/red-leader-website/app/(marketing)/services/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create blog posts data file</name>
  <files>app/data/blog-posts.ts</files>
  <action>
Create BlogPost interface and blogPosts array following the pattern in services.ts and case-studies.ts.

Interface BlogPost should include:
- id: string
- slug: string
- title: string
- excerpt: string (2-3 sentences for listing page)
- content: string (full markdown-style content with paragraphs)
- author: { name: string, role: string }
- publishedAt: string (ISO date format, e.g., "2025-01-15")
- category: string (e.g., "Infrastructure", "DevOps", "Security")
- tags: string[]
- readingTime: string (e.g., "8 min read")
- seoDescription: string (max 160 chars for meta description)

Create 3 initial blog posts with REAL technical depth (not placeholder content):

1. "Why Your Kubernetes Cluster Crashed at 3 AM (And How to Prevent It)"
   - Category: Infrastructure
   - Focus: Common K8s failure patterns (OOMKilled, pod eviction storms, etcd leader election issues)
   - Include specific commands, metrics to watch, and prevention strategies
   - ~1000-1200 words of actual content

2. "The Real Cost of 'We'll Fix It Later' Technical Debt"
   - Category: DevOps
   - Focus: Quantifying tech debt impact on incident response time, deployment velocity, and recovery
   - Real-world scenarios and metrics from infrastructure perspective
   - ~800-1000 words

3. "Zero-Downtime PostgreSQL Migrations: A Battle-Tested Guide"
   - Category: Infrastructure
   - Focus: Practical patterns for schema changes on production databases
   - Include logical replication, shadow tables, and rollback strategies
   - ~1000-1200 words

Helper functions to include:
- getBlogPostBySlug(slug: string): BlogPost | undefined
- getCategories(): string[]
- getRecentPosts(count: number): BlogPost[]
- getRelatedPosts(post: BlogPost): BlogPost[]
  </action>
  <verify>
    - File exists at app/data/blog-posts.ts
    - TypeScript compiles: `npx tsc --noEmit app/data/blog-posts.ts`
    - All 3 posts have meaningful content (not Lorem ipsum)
    - Each post has proper SEO metadata
  </verify>
  <done>BlogPost type exported, 3 posts with real technical content, helper functions work correctly</done>
</task>

<task type="auto">
  <name>Task 2: Create blog listing page</name>
  <files>app/(marketing)/blog/page.tsx</files>
  <action>
Create the blog listing page following the pattern in services/page.tsx and case-studies/page.tsx.

Requirements:
- export const dynamic = 'force-static'
- Metadata export with title "Blog - Red Leader" and description about technical insights
- Hero section with dark background (bg-brand-dark) matching other pages
- Grid of blog post cards (3 columns on lg, 2 on md, 1 on mobile)

Each post card should display:
- Category badge (bg-red-100 text-brand-red)
- Title (linked to /blog/[slug])
- Excerpt (line-clamp-3)
- Author name and role
- Published date (formatted nicely, e.g., "Jan 15, 2025")
- Reading time
- "Read More" link with arrow icon (matching case studies pattern)

Sort posts by publishedAt descending (newest first).

Include CTA section at bottom matching other pages pattern (Contact Us / Book a Consultation buttons).

Style consistency:
- Use existing Tailwind classes from services/page.tsx
- Text colors: text-brand-dark (headings), text-brand-gray (body), text-brand-red (accents)
- Hover states on cards: hover:shadow-md transition-shadow
  </action>
  <verify>
    - File exists at app/(marketing)/blog/page.tsx
    - Dev server runs without errors: `npm run dev` and visit /blog
    - Page shows all 3 blog posts with proper styling
    - Posts are sorted newest first
  </verify>
  <done>Blog listing page renders all posts with proper styling, metadata, and navigation</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- `/blog` page accessible and displays 3 posts
- Each post card shows: category, title, excerpt, author, date, reading time
- Clicking post title navigates to /blog/[slug] (will 404 until plan 02, that's expected)
- Page metadata visible in browser dev tools
</verification>

<success_criteria>
1. Blog data file exports BlogPost type and 3 posts with real technical content
2. Blog listing page displays all posts in a responsive grid
3. Posts sorted by publish date (newest first)
4. Each post has complete SEO metadata (title, description)
5. Page matches visual style of existing services/case-studies pages
</success_criteria>

<output>
After completion, create `.planning/phases/04-blog/04-01-SUMMARY.md`
</output>
